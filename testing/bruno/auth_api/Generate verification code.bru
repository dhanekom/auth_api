meta {
  name: Generate verification code
  type: http
  seq: 1
}

get {
  url: {{baseURL}}/api/auth/verify
  body: json
  auth: inherit
}

body:json {
  {
    "email": "test@gmail.com"
  }
}

vars:post-response {
  verification_code: res.body.data.verification_code
}
