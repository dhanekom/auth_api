meta {
  name: Verify user
  type: http
  seq: 1
}

post {
  url: {{baseURL}}/v1/auth/verify
  body: json
  auth: inherit
}

body:json {
  {
    "email": "test@gmail.com",
    "verification_code": "{{verification_code}}"
  }
}

vars:pre-request {
  : 
}
